@extends('layouts.app')
@section('title', 'Trabajos | Administración')
@section('content')
<section class="admin-page"><div class="container"><div class="page-heading"><div><p class="eyebrow">Operación</p><h1 class="page-title">Trabajos</h1></div></div><x-ui.card class="mb-4" padding="md"><form class="admin-filter-bar" method="GET"><input class="form-control form-control-sm" name="q" value="{{ request('q') }}" placeholder="Título o ID"><select class="form-select form-select-sm" name="status"><option value="">Todos los estados</option>@foreach (['searching','matched','awaiting_quote','pending','awaiting_payment','paid','on_the_way','arrived','in_progress','awaiting_confirmation','completed','expired','disputed','cancelled','rejected'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><button class="ui-button ui-button--primary ui-button--sm">Filtrar</button></form></x-ui.card><div class="admin-table-wrap"><table class="table admin-table"><thead><tr><th>ID / servicio</th><th>Participantes</th><th>Precio</th><th>Estado</th><th>Modo</th><th>Pago</th><th></th></tr></thead><tbody>@forelse ($jobs as $job)<tr><td>#{{ $job->id }}<small class="d-block text-muted">{{ $job->service?->title ?: $job->title }}</small></td><td>{{ $job->client->name }}<small class="d-block text-muted">{{ $job->professional?->user?->name ?? 'Sin profesional asignado' }}</small></td><td>{{ $job->formattedAgreedPrice() }}</td><td>{{ $job->status->value }}</td><td>{{ $job->service_mode?->value ?? 'scheduled' }}</td><td>{{ $job->payment?->status?->value ?: 'Sin pago' }}</td><td><a class="ui-button ui-button--outline ui-button--sm" href="{{ route('admin.jobs.show', $job) }}">Detalle</a></td></tr>@empty<tr><td colspan="7">Sin trabajos.</td></tr>@endforelse</tbody></table></div>@if ($jobs->hasPages())<div class="mt-4"><x-pagination :paginator="$jobs" /></div>@endif</div></section>
@endsection
