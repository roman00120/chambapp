@extends('layouts.app')
@section('title', 'Disputas | Administración')
@section('content')
<section class="admin-page"><div class="container"><div class="page-heading"><div><p class="eyebrow">Operación</p><h1 class="page-title">Disputas</h1></div></div><x-ui.card class="mb-4" padding="md"><form class="admin-filter-bar" method="GET"><select class="form-select form-select-sm" name="status"><option value="">Todos los estados</option>@foreach (['open','reviewing','resolved','rejected'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><button class="ui-button ui-button--primary ui-button--sm">Filtrar</button></form></x-ui.card><div class="admin-table-wrap"><table class="table admin-table"><thead><tr><th>Trabajo</th><th>Cliente</th><th>Profesional</th><th>Motivo</th><th>Estado</th><th></th></tr></thead><tbody>@forelse ($disputes as $dispute)<tr><td>#{{ $dispute->job_request_id }}</td><td>{{ $dispute->jobRequest->client->name }}</td><td>{{ $dispute->jobRequest->professional->user->name }}</td><td>{{ $dispute->reason }}</td><td>{{ $dispute->status->value }}</td><td><a class="ui-button ui-button--outline ui-button--sm" href="{{ route('admin.disputes.show', $dispute) }}">Revisar</a></td></tr>@empty<tr><td colspan="6">Sin disputas.</td></tr>@endforelse</tbody></table></div>@if ($disputes->hasPages())<div class="mt-4"><x-pagination :paginator="$disputes" /></div>@endif</div></section>
@endsection
