@extends('layouts.app')
@section('title', 'Reportes | Administración')
@section('content')
<section class="admin-page"><div class="container"><div class="page-heading"><div><p class="eyebrow">Moderación</p><h1 class="page-title">Reportes</h1></div></div><x-ui.card class="mb-4" padding="md"><form class="admin-filter-bar" method="GET"><select class="form-select form-select-sm" name="status"><option value="">Todos los estados</option>@foreach (['pending','reviewing','resolved','rejected'] as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><button class="ui-button ui-button--primary ui-button--sm">Filtrar</button></form></x-ui.card><div class="admin-table-wrap"><table class="table admin-table"><thead><tr><th>Reporte</th><th>Reportante</th><th>Recurso</th><th>Estado</th><th>Fecha</th><th></th></tr></thead><tbody>@forelse ($reports as $report)<tr><td>#{{ $report->id }} · {{ $report->reason }}</td><td>{{ $report->reporter->name }}</td><td>{{ class_basename($report->reportable_type) }} #{{ $report->reportable_id }}</td><td>{{ $report->status->value }}</td><td>{{ $report->created_at->format('d/m/Y H:i') }}</td><td><a class="ui-button ui-button--outline ui-button--sm" href="{{ route('admin.reports.show', $report) }}">Revisar</a></td></tr>@empty<tr><td colspan="6">Sin reportes.</td></tr>@endforelse</tbody></table></div>@if ($reports->hasPages())<div class="mt-4"><x-pagination :paginator="$reports" /></div>@endif</div></section>
@endsection
