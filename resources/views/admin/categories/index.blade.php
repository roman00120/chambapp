@extends('layouts.app')
@section('title', 'Categorías | Administración')
@section('content')
<section class="admin-page"><div class="container"><div class="page-heading"><div><p class="eyebrow">Catálogo</p><h1 class="page-title">Categorías</h1></div><a class="ui-button ui-button--primary" href="{{ route('admin.categories.create') }}">Nueva categoría</a></div><div class="admin-table-wrap"><table class="table admin-table"><thead><tr><th>Nombre</th><th>Slug</th><th>Servicios</th><th>Estado</th><th></th></tr></thead><tbody>@forelse ($categories as $category)<tr><td>{{ $category->name }}</td><td><code>{{ $category->slug }}</code></td><td>{{ $category->services_count }}</td><td>{{ $category->is_active ? 'Activa' : 'Inactiva' }}</td><td><div class="d-flex gap-2"><a class="ui-button ui-button--outline ui-button--sm" href="{{ route('admin.categories.edit', $category) }}">Editar</a><form method="POST" action="{{ route('admin.categories.toggle', $category) }}" data-confirm-form data-confirm-message="Confirma el cambio de estado." data-confirm-submit="Actualizar">@csrf @method('PATCH')<button class="ui-button ui-button--outline ui-button--sm" type="submit">{{ $category->is_active ? 'Desactivar' : 'Activar' }}</button></form></div></td></tr>@empty<tr><td colspan="5">Sin categorías.</td></tr>@endforelse</tbody></table></div>@if ($categories->hasPages())<div class="mt-4"><x-pagination :paginator="$categories" /></div>@endif</div></section>
@endsection
