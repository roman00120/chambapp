@extends('layouts.app')
@section('title', $category->exists ? 'Editar categoría' : 'Nueva categoría')
@section('content')
<section class="admin-page"><div class="container"><div class="admin-form-shell"><a class="text-link" href="{{ route('admin.categories.index') }}">← Categorías</a><h1 class="page-title h2 mt-3">{{ $category->exists ? 'Editar categoría' : 'Nueva categoría' }}</h1><x-ui.card class="mt-4" padding="lg"><form method="POST" action="{{ $category->exists ? route('admin.categories.update', $category) : route('admin.categories.store', $category) }}">@csrf @if ($category->exists) @method('PUT') @endif<div class="mb-3"><label class="form-label" for="category-name">Nombre</label><input class="form-control" id="category-name" name="name" value="{{ old('name', $category->name) }}" required maxlength="100"></div><div class="mb-3"><label class="form-label" for="category-description">Descripción</label><textarea class="form-control" id="category-description" name="description" maxlength="1000">{{ old('description', $category->description) }}</textarea></div><div class="mb-3"><label class="form-label" for="category-icon">Ícono</label><input class="form-control" id="category-icon" name="icon" value="{{ old('icon', $category->icon) }}" maxlength="80"></div><div class="mb-4"><label class="form-label" for="category-sort">Orden</label><input class="form-control" id="category-sort" name="sort_order" type="number" min="0" value="{{ old('sort_order', $category->sort_order ?? 0) }}" required></div><button class="ui-button ui-button--primary w-100" type="submit">Guardar categoría</button></form></x-ui.card></div></div></section>
@endsection
