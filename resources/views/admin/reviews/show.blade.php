@extends('layouts.app')
@section('title', 'Reseña #'.$review->id.' | Administración')
@section('content')
<section class="admin-page"><div class="container"><a class="text-link" href="{{ route('admin.reviews.index') }}">← Reseñas</a><x-ui.card class="mt-3" padding="lg"><p class="eyebrow">Reseña #{{ $review->id }}</p><h1 class="page-title h3">{{ $review->rating }} / 5 · {{ $review->client->name }}</h1><p>{{ $review->comment ?: 'Sin comentario.' }}</p><p>Profesional: {{ $review->professional->user->name }} · Trabajo: #{{ $review->job_request_id }}</p><p>Estado: {{ $review->is_hidden ? 'Oculta' : 'Visible' }}</p><form method="POST" action="{{ route('admin.reviews.moderate', $review) }}" data-confirm-form data-confirm-message="La moderación no cambia el rating original." data-confirm-submit="Confirmar moderación">@csrf @method('PATCH')<select class="form-select mb-2" name="action" required><option value="hide">Ocultar por incumplimiento</option><option value="restore">Restaurar reseña</option></select><textarea class="form-control mb-3" name="reason" maxlength="1000" placeholder="Motivo interno; requerido al ocultar"></textarea><button class="ui-button ui-button--primary" type="submit">Aplicar moderación</button></form></x-ui.card></div></section>
@endsection
