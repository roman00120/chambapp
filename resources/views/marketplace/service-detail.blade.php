@extends('layouts.public')

@section('title', $service->title.' | Chambapp')
@section('meta_description', \Illuminate\Support\Str::limit($service->description, 155))

@section('content')
    <section class="marketplace-page"><div class="container"><nav class="breadcrumb marketplace-breadcrumb" aria-label="Breadcrumb"><a href="{{ route('home') }}">Inicio</a><span>/</span><a href="{{ route('marketplace.search', ['category' => $service->category->slug]) }}">{{ $service->category->name }}</a><span>/</span><strong>Servicio</strong></nav><div class="row g-4 g-lg-5"><div class="col-12 col-lg-7">@if ($service->images->isNotEmpty())<div id="service-gallery-{{ $service->id }}" class="carousel slide service-gallery" data-bs-ride="false"><div class="carousel-inner">@foreach ($service->images->sortBy('sort_order') as $image)<div class="carousel-item {{ $loop->first ? 'active' : '' }}"><img src="{{ \Illuminate\Support\Facades\Storage::disk('public')->url($image->path) }}" class="service-gallery__image" alt="{{ $image->alt_text ?: $service->title }}" @if (!$loop->first) loading="lazy" @endif></div>@endforeach</div>@if ($service->images->count() > 1)<button class="carousel-control-prev" type="button" data-bs-target="#service-gallery-{{ $service->id }}" data-bs-slide="prev"><span class="carousel-control-prev-icon" aria-hidden="true"></span><span class="visually-hidden">Anterior</span></button><button class="carousel-control-next" type="button" data-bs-target="#service-gallery-{{ $service->id }}" data-bs-slide="next"><span class="carousel-control-next-icon" aria-hidden="true"></span><span class="visually-hidden">Siguiente</span></button>@endif</div>@else<div class="service-gallery service-gallery--empty"><i class="bi bi-tools" aria-hidden="true"></i><span>Este servicio aún no tiene imágenes.</span></div>@endif</div><div class="col-12 col-lg-5"><div class="service-detail__content"><span class="service-card__category">{{ $service->category->name }}</span><h1 class="page-title">{{ $service->title }}</h1><div class="service-detail__price mb-4">{{ $service->formattedPrice() }}</div><div class="service-detail__professional mb-4"><x-ui.avatar :user="$service->professional->user" :src="$service->professional->profile_photo" :name="$service->professional->user->name" size="md" /><div><span class="small text-muted d-block">Ofrecido por</span><a href="{{ route('professional.public-profile', $service->professional) }}">{{ $service->professional->user->name }}</a><x-ui.badge variant="verified" label="Verificado" dot /></div></div><div class="service-detail__facts"><span><i class="bi bi-geo-alt" aria-hidden="true"></i> {{ collect([$service->professional->city, $service->professional->state])->filter()->join(', ') ?: 'Cerca de ti' }}</span>@if ($service->professional->total_reviews > 0)<span><i class="bi bi-star-fill" aria-hidden="true"></i> {{ number_format((float) $service->professional->average_rating, 1) }} ({{ $service->professional->total_reviews }})</span>@else<span>Sin reseñas todavía</span>@endif</div><hr><h2 class="h5">Sobre este servicio</h2><p class="service-detail__description">{{ $service->description }}</p><div class="d-flex flex-column gap-2 mt-4"><a class="ui-button ui-button--primary w-100" href="{{ route('job-requests.create', $service) }}"><i class="bi bi-check2-circle" aria-hidden="true"></i> Contratar</a>@auth @if (auth()->user()->isClient())<form method="POST" action="{{ route('professional.favorite.toggle', $service->professional) }}">@csrf<button class="ui-button ui-button--outline w-100" type="submit"><i class="bi bi-heart{{ $isFavorite ? '-fill' : '' }}" aria-hidden="true"></i> {{ $isFavorite ? 'Quitar profesional de favoritos' : 'Guardar profesional en favoritos' }}</button></form>@endif @else<a class="ui-button ui-button--outline w-100" href="{{ route('login') }}"><i class="bi bi-heart" aria-hidden="true"></i> Inicia sesión para guardar favoritos</a>@endauth</div></div></div></div></div></section>
@endsection
