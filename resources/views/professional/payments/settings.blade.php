@extends('layouts.app')

@section('title', 'Pagos de Mercado Pago | Chambapp')

@section('content')
    <section class="payment-page"><div class="container"><div class="page-heading"><div><p class="eyebrow mb-2"><i class="bi bi-wallet2" aria-hidden="true"></i> Configuración</p><h1 class="page-title">Mercado Pago</h1><p class="section-copy mb-0">Conecta tu cuenta para recibir pagos mediante el marketplace.</p></div></div><x-ui.card class="mt-4" padding="lg"><div class="payment-connection-card"><span class="payment-placeholder__icon"><i class="bi bi-{{ $profile->isMercadoPagoConnected() ? 'check2-circle' : 'link-45deg' }}" aria-hidden="true"></i></span><div><h2 class="h4">{{ $profile->isMercadoPagoConnected() ? 'Mercado Pago conectado' : 'Mercado Pago no conectado' }}</h2><p class="text-muted mb-0">{{ $profile->isMercadoPagoConnected() ? 'Tu cuenta está preparada para recibir pagos de trabajos contratados.' : 'Conecta tu cuenta para que los clientes puedan pagar tus cotizaciones.' }}</p></div></div>@if ($errors->any())<x-ui.alert class="mt-4" variant="danger">{{ $errors->first() }}</x-ui.alert>@endif @if ($providerConfigured && ! $profile->isMercadoPagoConnected())<a class="ui-button ui-button--primary mt-4" href="{{ route('professional.payments.connect') }}"><i class="bi bi-box-arrow-up-right" aria-hidden="true"></i> Conectar Mercado Pago</a>@elseif (! $providerConfigured)<x-ui.alert class="mt-4" variant="warning">El administrador debe configurar las credenciales de Mercado Pago en el servidor.</x-ui.alert>@endif<p class="small text-muted mt-4 mb-0">Los tokens se almacenan cifrados y nunca se muestran en HTML, JSON o logs.</p></x-ui.card></div></section>
@endsection
